[workspace]
members = ["crabinfer-core", "crabinfer-server", "crabinfer-cli"]
resolver = "2"

[profile.release]
lto = "fat"
panic = "unwind"  # Must be unwind for UniFFI to catch panics at FFI boundary
strip = true
opt-level = 3
