---
phase: 01-model-loading-and-quantization
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crabinfer-core/src/serving/hub_download_tests.rs
  - crabinfer-core/src/serving/safetensors_loader_tests.rs
  - crabinfer-core/src/serving/quantization_tests.rs
autonomous: true
requirements: [MLOAD-01, MLOAD-02, MLOAD-03, QLOAD-01, QLOAD-02, QLOAD-03]

must_haves:
  truths:
    - "All 9 test stubs exist and fail with 'not yet implemented' before any Wave 1 work begins"
    - "Running cargo test shows RED for every stub, confirming Nyquist compliance"
  artifacts:
    - path: "crabinfer-core/src/serving/hub_download_tests.rs"
      provides: "Failing test stubs for HF Hub download behaviors"
      contains: "test_is_hf_repo_id"
    - path: "crabinfer-core/src/serving/safetensors_loader_tests.rs"
      provides: "Failing test stubs for GPTQ/AWQ config parsing and weight loading"
      contains: "test_gptq_config_parse"
    - path: "crabinfer-core/src/serving/quantization_tests.rs"
      provides: "Failing test stubs for Marlin reformat and dispatch"
      contains: "test_marlin_reformat"
  key_links: []
---

<objective>
Create failing test stubs for all 9 Phase 1 behaviors before any implementation begins, establishing Nyquist compliance.

Purpose: Wave 0 ensures every behavior has a RED test before the GREEN implementation in Waves 1-2. This catches missing coverage and enforces TDD discipline.
Output: Three test stub files with 9 failing tests covering hub download, GPTQ/AWQ loading, and Marlin kernel behaviors.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-model-loading-and-quantization/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test stubs for all Phase 1 behaviors</name>
  <files>crabinfer-core/src/serving/hub_download_tests.rs, crabinfer-core/src/serving/safetensors_loader_tests.rs, crabinfer-core/src/serving/quantization_tests.rs, crabinfer-core/src/serving/mod.rs</files>
  <action>
    Create three test stub files. Each test should compile but fail with `todo!("not yet implemented")` or `assert!(false, "stub — implement in Wave N")`. The tests define the expected behavior contract; Wave 1/2 plans will make them pass.

    NOTE: These files are standalone test modules. They will be wired into mod.rs as `#[cfg(test)]` modules. Alternatively, if the target source files (hub_download.rs, safetensors_loader.rs, quantization.rs) already have `#[cfg(test)] mod tests` blocks, add the stubs there instead. Use whichever approach compiles cleanly with the existing code.

    **hub_download stubs** (3 tests):
    1. `test_is_hf_repo_id_classification` — assert is_hf_repo_id("org/model") == true, is_hf_repo_id("/local/path") == false (stub: function doesn't exist yet, so use todo!())
    2. `test_ensure_model_cached_creates_local_dir` — assert ensure_model_cached returns a PathBuf pointing to ~/.cache/crabinfer/ (stub: function doesn't exist yet)
    3. `test_verify_sha256_rejects_corrupted_file` — create temp file, compute wrong hash, assert verify_sha256 returns Err (stub: function doesn't exist yet)

    **safetensors_loader stubs** (3 tests):
    4. `test_gptq_config_parse` — deserialize a GPTQ quantize_config.json string, assert bits=4, group_size=128 (stub: struct doesn't exist yet)
    5. `test_awq_config_parse` — deserialize AWQ config with q_group_size alias (stub: struct doesn't exist yet)
    6. `test_desc_act_true_rejected` — assert that desc_act=true config produces an error (stub: detection function doesn't exist yet)

    **quantization stubs** (3 tests):
    7. `test_marlin_reformat_aligned_produces_some` — create GptqLinear with N=64, K=16, call reformat_for_marlin, assert qweight_marlin is Some (stub: reformat is a stub returning false)
    8. `test_marlin_reformat_unaligned_skips` — create GptqLinear with N=100, assert qweight_marlin remains None
    9. `test_marlin_forward_dispatch_fallback` — assert forward() on non-CUDA device uses naive path (stub: forward doesn't check qweight_marlin yet)

    Each stub test must:
    - Compile successfully (`cargo test --no-run`)
    - Fail when run (`cargo test` shows FAILED)
    - Have a clear failure message indicating what Wave/Plan implements it

    Register test modules in mod.rs if needed (using `#[cfg(test)] mod hub_download_tests;` etc.).
  </action>
  <verify>
    <automated>cd /home/ubuntu/Development/crabinfer && cargo test -p crabinfer-core --no-run 2>&1 | tail -5 && echo "COMPILES" && cargo test -p crabinfer-core -- hub_download_tests safetensors_loader_tests quantization_tests --no-fail-fast 2>&1 | grep -E "(FAILED|test result)" | tail -10</automated>
  </verify>
  <done>
    - All 9 test stubs compile
    - All 9 test stubs fail (RED state)
    - Failure messages reference the implementing plan (e.g., "stub - implement in 01-01")
    - mod.rs registers the test modules
  </done>
</task>

</tasks>

<verification>
- `cargo test -p crabinfer-core --no-run` compiles without errors
- `cargo test -p crabinfer-core -- hub_download_tests safetensors_loader_tests quantization_tests` shows 9 FAILED tests
- No existing tests broken
</verification>

<success_criteria>
- 9 failing test stubs exist covering all Phase 1 behaviors
- Tests compile but fail, establishing RED baseline
- Nyquist compliance: every behavior has automated verification before implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-model-loading-and-quantization/01-00-SUMMARY.md`
</output>
