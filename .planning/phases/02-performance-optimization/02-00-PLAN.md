---
phase: 02-performance-optimization
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crabinfer-core/src/serving/arena.rs
  - crabinfer-core/src/serving/tokenizer_cache.rs
  - crabinfer-core/src/serving/mod.rs
autonomous: true
requirements: [KERN-01, KERN-02, MOPT-01, MOPT-02, MOPT-03, TOKN-01, TOKN-02]

must_haves:
  truths:
    - "Failing test stubs exist for every Phase 2 behavior before implementation begins"
  artifacts:
    - path: "crabinfer-core/src/serving/arena.rs"
      provides: "Arena allocator and buffer pool module with test stubs"
    - path: "crabinfer-core/src/serving/tokenizer_cache.rs"
      provides: "Tokenizer cache and parallel tokenization module with test stubs"
  key_links: []
---

<objective>
Create failing test stubs for all Phase 2 behaviors (Nyquist compliance). These tests define the expected behavior contracts before any implementation begins, ensuring every requirement has automated verification from the start.

Purpose: Nyquist Wave 0 — every subsequent plan has pre-existing tests to turn green.
Output: Test stubs in new modules (arena.rs, tokenizer_cache.rs) and existing modules (backend.rs).
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@crabinfer-core/src/serving/kernels/backend.rs
@crabinfer-core/src/serving/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for all Phase 2 behaviors</name>
  <files>
    crabinfer-core/src/serving/arena.rs,
    crabinfer-core/src/serving/tokenizer_cache.rs,
    crabinfer-core/src/serving/mod.rs,
    crabinfer-core/src/serving/kernels/backend.rs
  </files>
  <action>
Create two new modules and add test stubs across them and the existing backend module:

**1. `crabinfer-core/src/serving/arena.rs`** — New module for arena allocator + buffer pool.

Create the file with these test stubs (all must fail/panic with `todo!()`):

```rust
//! Arena allocator and tensor buffer pool for zero-allocation inference.

#[cfg(test)]
mod tests {
    // MOPT-01: Arena allocator
    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_arena_allocate_returns_subslice() {
        // Arena::new(capacity) then arena.allocate(size) returns a buffer
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_arena_reset_reuses_memory() {
        // After arena.reset(), next allocate reuses same base address
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_arena_oom_returns_error() {
        // Allocating more than capacity returns Err
        todo!()
    }

    // MOPT-02: Buffer pool
    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_buffer_pool_checkout_returns_tensor() {
        // BufferPool::new(shapes) then pool.checkout(shape) returns a pre-allocated tensor
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_buffer_pool_checkin_reuses_buffer() {
        // After pool.checkin(tensor), next checkout returns same storage
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_buffer_pool_shape_mismatch_allocates_new() {
        // Checking out a shape not in the pool still works (fallback alloc)
        todo!()
    }

    // MOPT-03: Zero alloc in hot path
    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_zero_alloc_forward_pass_uses_pool() {
        // A simulated forward step uses only pool buffers, no new allocations
        todo!()
    }
}
```

**2. `crabinfer-core/src/serving/tokenizer_cache.rs`** — New module for tokenizer caching + parallel tokenization.

```rust
//! Cached tokenizer with parallel encoding support.

#[cfg(test)]
mod tests {
    // TOKN-02: Tokenizer cache
    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_cached_tokenizer_returns_same_result() {
        // CachedTokenizer wraps a Tokenizer; encoding the same string twice returns identical token IDs
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_cached_tokenizer_cache_hit_is_faster() {
        // Second encode of same string is served from cache (no re-tokenization)
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_cached_tokenizer_lru_eviction() {
        // Cache with capacity N evicts LRU entry when N+1th unique string is encoded
        todo!()
    }

    // TOKN-01: Parallel tokenization
    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_parallel_encode_multiple_strings() {
        // encode_batch(&["hello", "world"]) returns vec of token ID vecs, one per input
        todo!()
    }

    #[test]
    #[should_panic(expected = "not yet implemented")]
    fn test_parallel_encode_scales_with_batch() {
        // Encoding N strings in parallel is faster than encoding N strings sequentially for large N
        todo!()
    }
}
```

**3. Update `crabinfer-core/src/serving/mod.rs`** — Add the two new modules:

```rust
pub mod arena;
pub mod tokenizer_cache;
```

**4. Add fused LayerNorm+linear test stub to `crabinfer-core/src/serving/kernels/backend.rs`** — Append to existing tests module:

```rust
// KERN-01 + KERN-02: Fused LayerNorm+linear
#[test]
#[should_panic(expected = "not yet implemented")]
fn test_fused_layernorm_linear_produces_correct_shape() {
    // backend.fused_layernorm_linear(x, norm_weight, eps, linear_weight) returns [batch, out_features]
    todo!()
}

#[test]
#[should_panic(expected = "not yet implemented")]
fn test_fused_layernorm_linear_matches_unfused() {
    // Fused result matches sequential rmsnorm then matmul within tolerance
    todo!()
}

#[test]
#[should_panic(expected = "not yet implemented")]
fn test_fused_layernorm_linear_default_fallback() {
    // Default trait impl uses unfused path (rmsnorm + matmul)
    todo!()
}
```

All tests MUST fail (via `todo!()` which panics with "not yet implemented"). Verify with `cargo test`.
  </action>
  <verify>
    <automated>cargo test -p crabinfer-core --no-default-features --lib serving::arena::tests -- --include-ignored 2>&1 | grep -c "test result" && cargo test -p crabinfer-core --no-default-features --lib serving::tokenizer_cache::tests -- --include-ignored 2>&1 | grep -c "test result" && cargo test -p crabinfer-core --no-default-features --lib serving::kernels::backend::tests::test_fused_layernorm -- --include-ignored 2>&1 | grep -c "test result"</automated>
  </verify>
  <done>All test stubs compile and fail with "not yet implemented". No existing tests broken. New modules registered in mod.rs.</done>
</task>

</tasks>

<verification>
- `cargo test -p crabinfer-core --no-default-features --lib serving` compiles successfully
- All new tests fail with `should_panic(expected = "not yet implemented")`
- All pre-existing tests still pass
</verification>

<success_criteria>
- 15 new test stubs created across 3 files
- All stubs fail as expected (todo!() panic)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-optimization/02-00-SUMMARY.md`
</output>
