---
phase: 01.2-bf16-fp16-serving-support
plan: "03"
subsystem: serving
tags: [rust, vram, bf16, fp16, gpu-memory, dtype]

# Dependency graph
requires:
  - phase: 01.2-02
    provides: resolve_serving_dtype and dtype-aware serving infrastructure
provides:
  - Dtype-parameterized weight memory estimation in gpu_memory.rs
  - weight_dtype_bytes threading from engine_loop through profile_gpu_memory
  - VRAM savings INFO log at engine startup for non-F32 serving dtypes

affects:
  - serving/engine_loop.rs
  - serving/gpu_memory.rs

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "weight_dtype_bytes threaded from resolved dtype through profiling APIs"
    - "savings_pct = (1.0 - bytes/4.0) * 100.0 for VRAM savings log"

key-files:
  created: []
  modified:
    - crabinfer-core/src/serving/gpu_memory.rs
    - crabinfer-core/src/serving/engine_loop.rs

key-decisions:
  - "weight_dtype_bytes added as final parameter to profile_gpu_memory (after kv_dtype_bytes) for API consistency"
  - "VRAM savings log guarded by serving_dtype != DType::F32 to avoid noisy F32-only startup logs"

patterns-established:
  - "Serving dtype size threaded via size_in_bytes() into memory profiling at engine startup"

requirements-completed:
  - DTYPE-01
  - DTYPE-02
  - DTYPE-03
  - DTYPE-04
  - DTYPE-05
  - DTYPE-06

# Metrics
duration: 8min
completed: 2026-04-04
---

# Phase 01.2 Plan 03: BF16/FP16 GPU Memory Profiling Gap Closure Summary

**Dtype-parameterized weight memory estimation: gpu_memory.rs now uses weight_dtype_bytes instead of hardcoded 2, with VRAM savings log in engine_loop.rs**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-04T13:15:00Z
- **Completed:** 2026-04-04T13:23:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- `estimate_weights_memory` now accepts `weight_dtype_bytes: usize` instead of hardcoding `* 2`, enabling accurate VRAM estimation for F32 (4 bytes), F16, and BF16 (2 bytes) serving dtypes
- `profile_gpu_memory` accepts and forwards `weight_dtype_bytes` as a new final parameter
- `engine_loop::start_inner` derives `weight_dtype_bytes = serving_dtype.size_in_bytes()` after `resolve_serving_dtype`, logs estimated VRAM savings vs F32 when dtype is not F32, and passes it to `profile_gpu_memory`
- All 805 workspace tests pass with no regressions

## Task Commits

1. **Task 1: Add weight_dtype_bytes to gpu_memory profiling and VRAM savings log** - `642dcae` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `crabinfer-core/src/serving/gpu_memory.rs` - estimate_weights_memory and profile_gpu_memory parameterized with weight_dtype_bytes; test call sites updated
- `crabinfer-core/src/serving/engine_loop.rs` - weight_dtype_bytes binding + VRAM savings INFO log after resolve_serving_dtype; weight_dtype_bytes passed to profile_gpu_memory

## Decisions Made
- `weight_dtype_bytes` added as the final parameter to `profile_gpu_memory` (after `kv_dtype_bytes`) to maintain parameter order consistency
- VRAM savings log is guarded with `if serving_dtype != DType::F32` to avoid unnecessary log noise in F32-only deployments

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None - build environment has pre-existing NCCL/nvcc linker errors on this Linux host (not caused by these changes); `cargo test --lib` confirms all 805 unit tests pass cleanly.

## Next Phase Readiness
- All Phase 01.2 verification gaps closed: weight memory estimation is dtype-aware, VRAM savings logged at startup for BF16/FP16
- Requirements DTYPE-01 through DTYPE-06 complete
- Phase 01.2 BF16/FP16 serving support is fully implemented

---
*Phase: 01.2-bf16-fp16-serving-support*
*Completed: 2026-04-04*
