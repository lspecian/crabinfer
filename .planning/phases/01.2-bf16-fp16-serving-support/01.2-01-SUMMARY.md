---
phase: 01.2-bf16-fp16-serving-support
plan: 01
subsystem: serving
tags: [dtype, cuda, bf16, f16, serving-engine, cli]

requires:
  - phase: 01.2-00
    provides: Wave 0 test stubs for dtype behaviors
provides:
  - ServingDType enum with Auto/F32/F16/BF16 variants
  - resolve_serving_dtype() with CUDA compute capability detection
  - is_fp32_weight() predicate for norm/embedding layers
  - --dtype CLI flag with config pipeline integration
  - KV cache Auto resolution coupled to serving dtype
affects: [01.2-02-weight-casting]

tech-stack:
  added: []
  patterns: [dtype-resolution-pattern, config-pipeline-pattern]

key-files:
  created: []
  modified:
    - crabinfer-core/src/serving/quantization.rs
    - crabinfer-core/src/serving/engine_loop.rs
    - crabinfer-server/src/config.rs
    - crabinfer-server/src/main.rs
    - crabinfer-server/src/lib.rs
    - crabinfer-cli/src/cmd_serve.rs

key-decisions:
  - "BF16 validation only on CUDA devices — CPU/Metal allow BF16 unconditionally"
  - "KV cache Auto now follows serving dtype instead of hardcoded F32"
  - "detect_best_dtype uses compute_capability: sm_80+ → BF16, sm_70+ → F16, else F32"

patterns-established:
  - "Dtype resolution: config string → ServingDType enum → resolve with device → candle DType"
  - "Config pipeline: TOML → env → CLI override, all as Option<String>"

requirements-completed: [DTYPE-01, DTYPE-02, DTYPE-05]

duration: 12min
completed: 2026-04-04
---

# Plan 01.2-01: ServingDType Enum + Config + CLI Summary

**ServingDType enum with CUDA-aware resolution, is_fp32_weight predicate, --dtype CLI flag, and KV cache coupling to serving dtype**

## Performance

- **Duration:** ~12 min
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- ServingDType enum (Auto/F32/F16/BF16) with FromStr, Display, Default traits
- resolve_serving_dtype() detects CUDA compute capability (Ampere+ → BF16, Volta+ → F16)
- BF16 on pre-Ampere returns actionable error suggesting --dtype f16
- is_fp32_weight() identifies norm/embedding layers for FP32 preservation
- KV cache Auto resolution now uses serving dtype instead of hardcoded F32
- --dtype CLI flag with full config pipeline (TOML, env var, CLI override)

## Task Commits

1. **Task 1: ServingDType enum, resolve_serving_dtype(), is_fp32_weight()** - `e8a49f7` (feat)
2. **Task 2: ServingEngineConfig field, KV cache coupling, --dtype CLI, config pipeline** - `533e525` (feat)
3. **Fix: Missing dtype field in crabinfer-cli CliOverrides** - `7ca822d` (fix)

## Files Created/Modified
- `crabinfer-core/src/serving/quantization.rs` - ServingDType enum, resolve, detect, is_fp32_weight
- `crabinfer-core/src/serving/engine_loop.rs` - serving_dtype field, KV cache coupling
- `crabinfer-server/src/config.rs` - dtype in CrabInferConfig, CliOverrides, env/CLI overrides
- `crabinfer-server/src/main.rs` - --dtype CLI argument
- `crabinfer-server/src/lib.rs` - dtype in ServerConfig, parse to ServingDType
- `crabinfer-cli/src/cmd_serve.rs` - dtype field in CLI overrides struct

## Decisions Made
- BF16 validation only enforced on CUDA devices (CPU/Metal support BF16 natively)
- Used cuda_stream().context().compute_capability() instead of BackendDevice trait

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Missing dtype field in crabinfer-cli**
- **Found during:** Task 2 (workspace test)
- **Issue:** CliOverrides struct in crabinfer-cli/src/cmd_serve.rs missing new dtype field
- **Fix:** Added `dtype: None` to the struct initialization
- **Verification:** Full workspace tests pass
- **Committed in:** 7ca822d

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential fix for compilation. No scope creep.

## Issues Encountered
- Agent hit permission restrictions mid-session; orchestrator completed commit and verification manually.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- ServingDType infrastructure complete, ready for Plan 02 to thread dtype through safetensors loader
- resolve_serving_dtype() available for model loading to determine weight cast target

---
*Phase: 01.2-bf16-fp16-serving-support*
*Completed: 2026-04-04*
