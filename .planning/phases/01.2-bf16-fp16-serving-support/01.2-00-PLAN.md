---
phase: 01.2-bf16-fp16-serving-support
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crabinfer-core/src/serving/quantization.rs
  - crabinfer-core/src/serving/safetensors_loader.rs
  - crabinfer-core/src/serving/engine_loop.rs
autonomous: true
requirements: [DTYPE-01, DTYPE-02, DTYPE-03, DTYPE-04, DTYPE-05, DTYPE-06]

must_haves:
  truths:
    - "Every DTYPE requirement has at least one failing test stub before implementation begins"
  artifacts:
    - path: "crabinfer-core/src/serving/quantization.rs"
      provides: "ServingDType enum tests and is_fp32_weight tests"
      contains: "test_serving_dtype_fromstr"
    - path: "crabinfer-core/src/serving/safetensors_loader.rs"
      provides: "load_tensor dtype test stub"
      contains: "test_load_tensor_dtype_cpu"
    - path: "crabinfer-core/src/serving/engine_loop.rs"
      provides: "KV cache dtype coupling test"
      contains: "test_kv_dtype_auto_follows_serving_dtype"
  key_links: []
---

<objective>
Wave 0: Create failing test stubs for all DTYPE-01 through DTYPE-06 behaviors before any implementation.

Purpose: Nyquist compliance -- every requirement has a test before code is written.
Output: Test stubs in quantization.rs, safetensors_loader.rs, and engine_loop.rs that compile but fail at runtime.
</objective>

<execution_context>
@/home/ubuntu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-bf16-fp16-serving-support/01.2-CONTEXT.md
@.planning/phases/01.2-bf16-fp16-serving-support/01.2-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ServingDType and is_fp32_weight test stubs to quantization.rs</name>
  <files>crabinfer-core/src/serving/quantization.rs</files>
  <action>
Add a `#[cfg(test)] mod serving_dtype_tests` block at the bottom of quantization.rs with these failing test stubs using `todo!("not yet implemented")`:

1. `test_serving_dtype_fromstr` (DTYPE-02): Assert `"auto".parse::<ServingDType>()` returns `Auto`, `"bf16"` returns `BF16`, `"f16"` returns `F16`, `"f32"` returns `F32`, `"invalid"` returns Err.
2. `test_serving_dtype_display` (DTYPE-02): Assert Display output for each variant.
3. `test_serving_dtype_default` (DTYPE-02): Assert `ServingDType::default()` is `Auto`.
4. `test_is_fp32_weight_embed_tokens` (DTYPE-04): Assert `is_fp32_weight("model.embed_tokens.weight")` is true.
5. `test_is_fp32_weight_rms_norm` (DTYPE-04): Assert `is_fp32_weight("model.layers.0.input_layernorm.weight")` is true, `is_fp32_weight("model.norm.weight")` is true.
6. `test_is_fp32_weight_linear_proj` (DTYPE-04): Assert `is_fp32_weight("model.layers.0.self_attn.q_proj.weight")` is false (negative case).
7. `test_resolve_serving_dtype_auto_cpu` (DTYPE-01): Assert `resolve_serving_dtype(ServingDType::Auto, &Device::Cpu)` returns F32.
8. `test_resolve_serving_dtype_explicit_f16` (DTYPE-01): Assert `resolve_serving_dtype(ServingDType::F16, &Device::Cpu)` returns F16.

Each test should use `#[should_panic(expected = "not yet implemented")]` and call `todo!()` since the functions and types do not exist yet. The test module references `ServingDType`, `is_fp32_weight`, and `resolve_serving_dtype` which will be created in Plan 01.

Since the types don't exist yet, each test body should just be `todo!("not yet implemented")` -- the actual assertions are written as comments above the `todo!()` so Plan 01 can replace `todo!()` with real assertions.
  </action>
  <verify>
    <automated>cd /home/ubuntu/Development/crabinfer && cargo test -p crabinfer-core serving_dtype_tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>8 test stubs exist in quantization.rs, all compile and fail with "not yet implemented" panic. Covers DTYPE-01, DTYPE-02, DTYPE-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add KV cache coupling and load_tensor dtype test stubs</name>
  <files>crabinfer-core/src/serving/engine_loop.rs, crabinfer-core/src/serving/safetensors_loader.rs</files>
  <action>
In engine_loop.rs, find the existing `#[cfg(test)]` module (or create one). Add these failing test stubs:

1. `test_kv_dtype_auto_follows_serving_dtype_bf16` (DTYPE-05): Comment describing: when serving_dtype is BF16, `KVCacheDType::Auto.resolve(serving_dtype)` should return BF16. Body: `todo!("not yet implemented")`.
2. `test_kv_dtype_auto_follows_serving_dtype_f16` (DTYPE-05): Same for F16.
3. `test_kv_cache_explicit_override_ignores_serving_dtype` (DTYPE-05): Comment: `KVCacheDType::F16.resolve(serving_dtype_bf16)` should return F16. Body: `todo!()`.
4. `test_default_config_has_serving_dtype_auto` (DTYPE-02): Comment: `ServingEngineConfig::default().serving_dtype` should be `ServingDType::Auto`. Body: `todo!()`.

In safetensors_loader.rs, find the existing `#[cfg(test)]` module (or create one). Add:

5. `test_load_tensor_dtype_bf16_cpu` (DTYPE-03): Comment: `load_tensor(weights, name, &Device::Cpu, DType::BF16)` should return a BF16 tensor. Body: `todo!()`.
6. `test_load_tensor_fp32_preserves_dtype` (DTYPE-03): Comment: `load_tensor_fp32(weights, name, &Device::Cpu)` should return F32 regardless. Body: `todo!()`.
7. `test_load_tensor_cross_cast_skip` (DTYPE-03): Comment: If tensor on disk is already BF16, passing F16 target should NOT cross-cast. Body: `todo!()`.

All tests use `#[should_panic(expected = "not yet implemented")]`.

For DTYPE-06 (forward pass in serving dtype): this is an integration-level behavior that is automatic once weights are BF16 -- candle matmul uses the tensor's native dtype. Add a comment in the engine_loop test module noting this: `// DTYPE-06: Forward pass dtype is automatic -- candle matmul uses tensor's native dtype. No dedicated test needed; validated by integration testing on GPU.`
  </action>
  <verify>
    <automated>cd /home/ubuntu/Development/crabinfer && cargo test -p crabinfer-core test_kv_dtype_auto test_default_config_has test_load_tensor_dtype test_load_tensor_fp32 test_load_tensor_cross -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>7 test stubs across engine_loop.rs and safetensors_loader.rs, all compile and fail with "not yet implemented". Covers DTYPE-03, DTYPE-05, DTYPE-06 (comment). All 6 DTYPE requirements have test coverage mapped.</done>
</task>

</tasks>

<verification>
All 15 test stubs compile and panic with "not yet implemented":
```
cargo test -p crabinfer-core serving_dtype_tests test_kv_dtype test_default_config_has test_load_tensor -- 2>&1 | grep "test result"
```
Expected: 15 tests run, all panicked (passed due to should_panic).
</verification>

<success_criteria>
- Every DTYPE requirement (01-06) has at least one test stub
- All stubs compile without errors
- All stubs fail at runtime with "not yet implemented"
- No production code is changed
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-bf16-fp16-serving-support/01.2-00-SUMMARY.md`
</output>
